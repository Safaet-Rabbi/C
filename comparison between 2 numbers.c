#include<stdio.h>
int main()
{
 int a,b;
 scanf("%d %d",&a,&b);
 if(a>b)
 {
 printf("%d is greater than %d",a,b);
  }
  else if(b>a)
  {
   printf("%d is greater than%d",a,b);
  }
  else
  {
   printf("%d is equal %d",a,b);
  }
  return 0;
}
